name: Update README

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    name: Update this repo's README with recent activity

    steps:
      - uses: actions/checkout@v3
      - uses: manueldevjour/github-activity-readme@master
        env:
          GITHUB_TOKEN: ${{ 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 }}